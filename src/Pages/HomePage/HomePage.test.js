// import React from "react";
// import { render, screen } from "@testing-library/react";
// import HomePage from "./HomePage";

// test("renders homepage component", () => {
//   // Render the component
//   render(<HomePage />);

//   // Check if Navbar component is rendered
//   const navbarElement = screen.getByLabelText("navbar");
//   expect(navbarElement).toBeInTheDocument();

//   // Check if Header component is rendered
//   const headerElement = screen.getByLabelText("header");
//   expect(headerElement).toBeInTheDocument();

//   // Check if VideoCard components are rendered
//   const videoCards = screen.getAllByLabelText("video-card");
//   expect(videoCards.length).toBe(4);
// });
